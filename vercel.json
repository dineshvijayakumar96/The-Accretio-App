{
    "builds": [{
        "src": "accretio/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "270mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "accretio/wsgi.py"
        }
    ]
}